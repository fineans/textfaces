function drawFace(face){var canvas=document.querySelector('canvas');var ctx=canvas.getContext('2d');var cHeight=2500;var cWidth=2000;var textSize=500;ctx.font='bold '+textSize+'px Droid Sans';while(ctx.measureText(face).width>cWidth){textSize-=10;ctx.font='bold '+textSize+'px Droid Sans';}
var width=ctx.measureText(face).width;console.log(width);ctx.fillText(face,1000-width/2,1250,2000);var img=canvas.toDataURL("image/png");return img;}
var saveAllFaceImages=function(){$('button.facebtn').each(function(){var faceid=$(this).attr("face-id");var face=$(this).attr("data-clipboard-text");console.log(face);var image=drawFace(face);var canvas=document.querySelector('canvas');var ctx=canvas.getContext('2d');ctx.clearRect(0,0,canvas.width,canvas.height);$.post("http://localhost:5000/recieveimage",{faceid:faceid,img:image});});}